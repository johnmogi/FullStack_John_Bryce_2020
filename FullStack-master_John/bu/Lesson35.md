export class About extends Component {

```
public componentWillMount(){
    console.log("componentWillMount")
}

public componentDidMount(){
    console.log("componentDidMount")
}
public componentWillUnmount(){
    console.log("componentWillUnmount")
}

יש לי אפשרות להעביר נתונים לפני הרכיב יוצא משימוש.

Life cycles
נקודות התיחסות בחיי הרכיב בהקשר של הרכיב
```

npm i : install -g: global הרמת שרת

```
npm i -g json-server
מתקין שרת ע"מ לבצע בדיקות מקומיות

json-server products.json --port 3015


Model
מחלקת מידע טהור
לדוגמא: מוצר, לקוח, ספק, עובד, חתול



PARENT
PROPS >> CHILD

CHILD >> קריאה לפונקציה שנמצאת בהורה
ההורה צריך לתת את הפונקציה ל
CHILD
ב-
PROPS

שליחת מידע מה ילד להורה


הציגו גם את התמונה - מתחת לפרטים ללא ה
thumbnail
מתחת לתמונה הוסיפו לינק
Back to list

 הסטייט של הקומפוננט ה המידע החדש שאני מנהל
```
